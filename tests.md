pytest -m unit        # only unit tests
pytest -m integration # only integration tests
pytest -m chaos       # only chaos tests
pytest -m contract    # only contract tests
pytest -m saga        # only sagas
